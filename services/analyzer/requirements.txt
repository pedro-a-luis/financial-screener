# Analyzer Service Dependencies (Celery Workers with Polars)
# Python 3.11+

# Data processing - Polars (blazingly fast)
polars==1.17.1
polars-lts-cpu==1.17.1  # CPU-optimized for ARM64

# Distributed task queue
celery==5.4.0
redis==5.2.0  # Message broker and result backend

# Database
asyncpg==0.30.0  # Async PostgreSQL
# connectorx==0.4.0  # Fast PostgreSQL → Polars connector (no ARM64 wheels, using psycopg2 instead)
psycopg2-binary==2.9.10  # PostgreSQL driver

# Numerical computing (for custom calculations)
numpy==2.1.3

# Configuration
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Logging
structlog==24.4.0

# Monitoring
flower==2.0.1  # Celery monitoring dashboard

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-celery==1.1.3
pytest-cov==6.0.0
